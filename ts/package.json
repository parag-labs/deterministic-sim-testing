{
  "name": "deterministic-sim-testing",
  "version": "0.1.0",
  "description": "Deterministic simulation testing for distributed code (TypeScript port).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
